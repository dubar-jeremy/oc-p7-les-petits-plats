/**
 * This function will compare two recipes
 * It will return true if the recipes are the same
 * It will return false if the recipes are not the same
 */
export function compareRecipes(recipe1, recipe2) {
    return recipe1.name === recipe2.name
}
